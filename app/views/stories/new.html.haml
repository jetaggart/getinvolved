= semantic_form_for @story do |f|
  = f.input :title, :label => "Name your story"
  = f.date_select :story_date, :label => "When will this take place?"
  = f.input :description, :label => "Describe your story"
  = f.submit :value => "Create Story"
